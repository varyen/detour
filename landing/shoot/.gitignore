# Локальное окружение съёмки: браузер, сессия панели и — главное — карта
# подстановок и выгрузки ответов. В них РЕАЛЬНЫЕ имена профилей, серверы и
# адреса подписок, коммитить нельзя (см. «Персональные данные» в CLAUDE.md).
node_modules/
package-lock.json
session.txt
map.json
dump/
local_overrides.json
